{
  "name": "@actsecurity/iam-lens",
  "version": "0.1.158",
  "description": "Visibility in IAM in and across AWS accounts",
  "keywords": [
    "aws",
    "iam",
    "security",
    "identity"
  ],
  "homepage": "https://github.com/act-security-labs/iam-lens#readme",
  "bin": {
    "iam-lens": "dist/esm/cli.js"
  },
  "exports": {
    ".": {
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "files": [
    "dist/**/*"
  ],
  "types": "dist/cjs/index.d.ts",
  "prettier": "@actsecurity/prettier-config",
  "bugs": {
    "url": "https://github.com/act-security-labs/iam-lens/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/act-security-labs/iam-lens.git"
  },
  "license": "AGPL-3.0-or-later",
  "author": "David Kerber <dave@cloudcopilot.io>",
  "scripts": {
    "build": "npx tsc -p tsconfig.cjs.json && npx tsc -p tsconfig.esm.json && ./postbuild.sh",
    "clean": "rm -rf dist",
    "test": "npx vitest --run --coverage",
    "release": "npm install && npm run clean && npm run build && npm test && npm run format-check && npm publish",
    "format": "npx prettier --write src/",
    "format-check": "npx prettier --check src/"
  },
  "devDependencies": {
    "@actsecurity/prettier-config": "^0.1.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.6",
    "@semantic-release/npm": "^13.1.4",
    "@semantic-release/release-notes-generator": "^14.0.3",
    "@types/node": "^22.5.0",
    "@vitest/coverage-v8": "^4.0.18",
    "semantic-release": "^25.0.3",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18"
  },
  "release": {
    "branches": [
      "main"
    ],
    "plugins": [
      [
        "@semantic-release/commit-analyzer",
        {
          "releaseRules": [
            {
              "type": "feat",
              "release": "patch"
            },
            {
              "type": "fix",
              "release": "patch"
            },
            {
              "breaking": true,
              "release": "patch"
            },
            {
              "type": "*",
              "release": "patch"
            }
          ]
        }
      ],
      "@semantic-release/release-notes-generator",
      "@semantic-release/changelog",
      [
        "@semantic-release/npm",
        {
          "npmPublish": true
        }
      ],
      [
        "@semantic-release/exec",
        {
          "successCmd": "echo published=true >> $GITHUB_OUTPUT && echo version=${nextRelease.version} >> $GITHUB_OUTPUT && echo package_name=$(node -p \"require('./package.json').name\") >> $GITHUB_OUTPUT"
        }
      ],
      [
        "@semantic-release/git",
        {
          "assets": [
            "package.json",
            "package-lock.json",
            "CHANGELOG.md"
          ],
          "message": "chore(release): ${nextRelease.version} [skip ci]"
        }
      ],
      [
        "@semantic-release/github",
        {
          "assets": []
        }
      ]
    ]
  },
  "dependencies": {
    "@actsecurity/cli": "^0.2.23",
    "@actsecurity/iam-collect": "^0.1.216",
    "@actsecurity/iam-data": ">=0.15.202511222 <1.0.0",
    "@actsecurity/iam-expand": "^0.11.29",
    "@actsecurity/iam-policy": "^0.1.89",
    "@actsecurity/iam-shrink": "^0.1.21",
    "@actsecurity/iam-simulate": "^0.1.166",
    "@actsecurity/iam-utils": "^0.1.78",
    "@actsecurity/job": "^0.1.65",
    "@actsecurity/log": "^0.1.60",
    "bitset": "^5.2.3"
  }
}
